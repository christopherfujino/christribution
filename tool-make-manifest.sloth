#!/usr/bin/env sloth

let projectName = "go-bootstrapper"

with($cwd = "${ $scriptDir }/${ projectName }") {
  let run = func(subcommand) {
    "./${projectName} ${subcommand}"!
  }
  let result = "go build"!
  run("bootstrap")
  run("fetch")
  run("extract")
}
