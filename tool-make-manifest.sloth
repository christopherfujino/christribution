#!/usr/bin/env sloth-unsafe

let projectName = "go-bootstrapper"

with($cwd = "./${projectName}") {
  let run = func(subcommand) {
    "./${projectName} ${subcommand}"!
  }
  let result = "go build"!
  run("bootstrap")
  run("fetch")
  run("extract")
}
